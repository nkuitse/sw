#!/usr/bin/perl

use strict;
use warnings;

my %sub = map { split /=/, $_, 2 } @ARGV;

while (<STDIN>) {
    s{
        ^
        use constant ([A-Z_]+) => '[^']+';
    }{
        sprintf q{use constant %s => '%s';}, $1, $sub{$1} || $1;
    }egx;
    print;
}
